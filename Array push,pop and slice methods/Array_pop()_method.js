


function func() {
    var arr = ['NRS', 'NIT', 'LOH', 'NRSTechCode'];
    
    // Popping the last element from the array
    console.log(arr.pop());
    }
    func();

    function func() {
        var arr = [34, 234, 567, 4];
     
        // Popping the last element from the array
        var popped = arr.pop();
        console.log(popped);
        console.log(arr);
    }
    func();

    function func() {
        var arr = [];
     
        // popping the last element
        var popped = arr.pop();
        console.log(popped);
    }
    func();